apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

generatorOptions:
  disableNameSuffixHash: true

secretGenerator:
  - name: processor-scripts
    files:
      - entrypoint.sh

resources:
  - deployment.yaml
